#### data structure 数据结构

``` 
优质博客文章：Java数据结构和算法系列
http://www.cnblogs.com/ysocean/tag/Java%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E5%92%8C%E7%AE%97%E6%B3%95/

基础的数据结构：
1.Array 数组 
  Array 无序数组
  Sorted Array 有序数组
2.Linked 链表 
3.Queue 队列 (先进先出FIFO)
4.Stack 栈 (后进先出LIFO)
5.Hash 哈希表
6.Tree 树 
  二叉树
  红黑树
  2-3-4树
7.Heap 堆
8.Grap 图

数据结构的基本功能：
insert|add 插入、
find 查找通过值、get 查找通过索引、
delete 删除、
modity 修改、
foreach 迭代访问
```
- 1.Array 数组
``` 
数组是数据结构的鼻祖，数组是一种通用的数据结构，能用来实现队列、栈等很多数据结构。
是一种线性结构。
1.Array 无序数组
  特征：插入快O(1) 查找索引快O(1)值慢O(n) 删除慢(先查找再移动很耗时)
2.Sorted Array 有序数组
  要实现有序数组，就要用到排序算法，可以查看算法笔记
  特征：插入慢(每次插入都要重排序(数组排序意味着要移动)) 查找快(二分查找) 删除慢(先查找再移动很耗时)
```

- 2.Linked 链表 
```
链表也是一种使用广泛的通用数据结构，也可以用来作为实现队列、栈等数据结构的基础。
链表通常由一连串节点组成，每个节点包含任意的实例数据（data fields）
和一或两个用来指向上一个/或下一个节点的位置的链接（"links"）。
是一种线性结构，但不是线性的顺序存储数据，而是在每一个节点里存到下一个节点的指针(Pointer)。

使用链表结构可以克服数组链表需要预先知道数据大小的缺点，链表结构可以充分利用计算机内存空间，实现灵活的内存动态管理。
但是链表失去了数组随机读取的优点，同时链表由于增加了结点的指针域，空间开销比较大。

1.Single-Linked 单向链表：
  节点(Node)：(data)保存或者显示关于节点的信息、存储下一个节点的地址。最后一个节点存储地址的部分指向null。
  单向链表只可向一个方向遍历，查找要从第一个节点开始遍历，一直访问到需要的位置。
  插入一个节点，对于单向链表，我们只提供在链表头插入，只需要将当前插入的节点设置为头节点，next指向原头节点即可。
  删除一个节点，我们将该节点的上一个节点的next指向该节点的下一个节点。
  
  addHead(Object obj) find(Object obj) deleteHead() delete(Object obj)
  
2.双端链表：
  对于单项链表，我们如果想在尾部添加一个节点，那么必须从头部一直遍历到尾部，找到尾节点，然后在尾节点后面插入一个节点。
  这样操作很麻烦，如果我们在设计链表的时候多个对尾节点的引用，那么会简单很多。
  即尾节点(tail)存储下一个节点的地址指向头节点(head)
  addHead(Object obj) addTail(Object obj) deleteHead() deleteTail()其他和单向链表一样
  
3.有序链表：(前面的都是无序的) 在有些应用中需要链表中的数据有序，这称为有序链表。
  在有序链表中，数据是按照关键值有序排列的。
  一般在大多数需要使用有序数组的场合也可以使用有序链表。
  有序链表优于有序数组的地方是插入的速度（因为元素不需要移动，虽然要排序但不需要移动），
  另外链表可以扩展到全部有效的使用内存，而数组只能局限于一个固定的大小中。
  
  插入，并按从小到大排序
  删除头节点()
  insert(Object obj) deleteHead()
```

- 3.Queue 队列
```
是一种线性结构。
用双端链表实现队列：
  队列的insert()、delete()，
  对应于链表的addHead(Object obj)、deleteTail()
  或者addTail(Object obj)、deleteHead()
```

- 4.Stack 栈
``` 
是一种线性结构。
用单向链表实现栈：
  栈的push()入栈方法、pop()出栈方法，
  对应于链表的头部增加元素addHead(Object obj)、在头部删除元素deleteHead()
```
